# API Reference

本项目 API 文档由 Doxygen 生成。

- 如果你通过 CMake 生成：

```bash
cmake --preset ninja-debug -DCPP_QUICK_STARTER_BUILD_DOCS=ON
cmake --build --preset ninja-debug --target doxygen
```

- 生成后可通过以下路径访问 HTML：

`api/doxygen/html/index.html`
